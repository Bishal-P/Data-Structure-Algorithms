#include<iostream>
#include<string>
#include<math.h>

using namespace std;

int main(){

    char binary[]="11111";
    int len=binary.length(),integer=0;
    for(int i=0;i<len;i++){
        char num=binary[1];
        // int t=stoi(num);
        // integer+=pow(2,num);
        cout<<num;
    }
    // cout<<integer;

    
}